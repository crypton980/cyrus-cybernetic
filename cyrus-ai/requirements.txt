chromadb>=0.5.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
numpy>=1.26.0
pydantic>=2.7.0
python-multipart>=0.0.9
httpx>=0.27.0
openai>=1.30.0
redis>=5.0.0
tiktoken>=0.7.0
# ── Local model inference + fine-tuning (optional — install when GPU available)
# These packages are large; service starts without them (graceful degradation).
transformers>=4.40.0
torch>=2.2.0
accelerate>=0.29.0
datasets>=2.19.0

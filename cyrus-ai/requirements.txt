chromadb>=0.5.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
numpy>=1.26.0
pydantic>=2.7.0
python-multipart>=0.0.9
httpx>=0.27.0
openai>=1.30.0
redis>=5.0.0
tiktoken>=0.7.0

# ── Observability ─────────────────────────────────────────────────────────────
loguru>=0.7.0
prometheus-client>=0.20.0

# ── Distributed tracing (optional — service starts without them) ──────────────
# Install for full OpenTelemetry tracing support.
opentelemetry-sdk>=1.24.0
opentelemetry-instrumentation-fastapi>=0.45b0

# ── Local model inference + fine-tuning (optional — install when GPU available)
# These packages are large; service starts without them (graceful degradation).
transformers>=4.40.0
torch>=2.2.0
accelerate>=0.29.0
datasets>=2.19.0
